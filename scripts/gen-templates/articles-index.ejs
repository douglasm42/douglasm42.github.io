export interface ArticleMetadata {
  title: string,
  tags: string[],
  createdAt: string
}

const articles = [<% pages.forEach( page => {%>
  {
    metadataPath: '<%- root %>/<%- page.metadataPath %>',
    markdownPath: '<%- root %>/<%- page.markdownPath %>',
  },<% }) %>
]

export default articles
